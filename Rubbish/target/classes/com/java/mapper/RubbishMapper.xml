<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "com.java.mapper.RubbishMapper">

    <!-- 查询管理员 -->
    <select id="getrubbish" resultType="Rubbish">
        select rubbish.ru_class,rubbish.ru_name,
        class.class_name from rubbish left join `class`
        on rubbish.ru_class=class.class_id
        where rubbish.ru_name like concat('%',#{RuName},'%')
    </select>

</mapper>